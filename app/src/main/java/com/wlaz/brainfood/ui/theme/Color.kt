package com.wlaz.brainfood.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════
// BrainFood — Monochrome B&W Premium Palette
// ═══════════════════════════════════════════════

// Fondos
val BrainFoodBlack       = Color(0xFF080808) // Negro puro
val BrainFoodDarkGray    = Color(0xFF111111) // Superficie oscura
val BrainFoodCardBg      = Color(0xFF161616) // Fondo de tarjetas

// Acento Principal — Blanco
val BrainFoodGreen       = Color(0xFFFFFFFF) // Acento principal → Blanco
val BrainFoodGreenDark   = Color(0xFF333333) // Para fondos activos
val BrainFoodGreenLight  = Color(0xFFE0E0E0) // Para highlights
val BrainFoodGreenSurface = Color(0xFF0E0E0E) // Superficie very dark

// Legacy alias
val BrainFoodNeonGreen   = BrainFoodGreen

// Secundarios
val BrainFoodSecondary   = Color(0xFF888888) // Gris medio
val BrainFoodTertiary    = Color(0xFFAAAAAA) // Gris claro

// Textos
val TextPrimary   = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB0B0B0) // Gris claro (mejor contraste)
val TextHint      = Color(0xFF4A4A4A) // Hint gris apagado

// Semánticos
val MatchGreen  = Color(0xFFFFFFFF) // Match perfecto → Blanco
val MatchOrange = Color(0xFF999999) // Advertencia → Gris
val MatchRed    = Color(0xFF555555) // Falta → Gris oscuro

// Estados de ingrediente
val StatusInStock   = Color(0xFFFFFFFF) // En stock → Blanco
val StatusLowStock  = Color(0xFF999999) // Pocas unidades → Gris
val StatusExpiring  = Color(0xFF555555) // Expirando → Gris oscuro

// Categorías de ingredientes — mantenemos colores sutiles para variedad visual
val CategoryProtein = Color(0xFFFF6B6B) // Rojo cálido
val CategoryVeggie  = Color(0xFF51CF66) // Verde fresco
val CategoryGrain   = Color(0xFFFFD43B) // Dorado
val CategoryDairy   = Color(0xFF74C0FC) // Azul claro
val CategorySpice   = Color(0xFFFF922B) // Naranja especias
val CategoryOther   = Color(0xFFB197FC) // Lavanda

// Gradientes
val GradientStart = Color(0xFF080808)
val GradientEnd   = Color(0xFF000000)
